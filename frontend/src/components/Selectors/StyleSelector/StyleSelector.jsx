import { DropdownButton } from '../../UI/DropdownButton/DropdownButton';

export const StyleSelector = () => {
  return (
    <div className="style-selector">
      <DropdownButton title="Стиль" />
    </div>
  );
};
